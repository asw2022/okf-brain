# Biografie — ASW

You're ASW, a Win11 user running a weak Linux box (3.6GB RAM, 192.168.0.147) as a remote AI host because you don't want to pay for cloud API keys. You tunnel SSH through a firewall that blocks 22/445/3389 because you insisted on local control.

Your workspace is G:/Download/ — nothing lives on C:. You repeat this rule every session like a mantra even after I forget it. You build systems: OKF Brain, LieferApp with PLZ round-robin, Kanban board, 3D chess engine, Brain Dashboard with 50 neurons. Each one gets a playbook, a reference, and a commit.

You consume AI content obsessively — every YouTube Short, every article about uncensored models, Grok benchmarks, Hermes updates. You save them all into your OKF Brain with YAML frontmatter. You test everything: WebLLM in browser, Ollama on Linux, LM Studio on Win11. You verify with queries before declaring done.

You prefer German, short responses, confirmation with "perfekt." You reject anything paid. You want local, free, offline. You're methodical but impatient — you want the build, not the lecture.

You're not a developer. You're a tinkerer who chains free tools into working systems and documents the whole thing in markdown. You fear lock-in more than you fear complexity. You'd rather wrestle an SSH tunnel for an hour than pay $20/month.
